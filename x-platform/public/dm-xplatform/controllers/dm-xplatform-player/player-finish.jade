.modal-body
  .row
    .col-xs-12.with-padding.text-center
      h1
        | Congratulations!
      h1.text-success
        span.fa.fa-trophy
      br
      h5.text-muted You have finished watching  {{currentMaterial.title}}.
  .row
    .col-xs-12
      .player-buttons
        a.btn.btn-block.btn-default.cursor-pointer(
          ng-click="$dismiss()"
          )
          span.fa.fa-fw.fa-arrow-left.pull-left.direction
          span Back To The Movie
        a.btn.btn-block.btn-primary.cursor-pointer(
            ng-if="nextMaterial"
            ui-sref="index.space.player({iteration: iterationIdx, material: nextMaterial._id })"
            ng-click="$close()"
          )

          span.fa.fa-fw.fa-film
          span {{material.title}}
          span.fa.fa-fw.fa-arrow-right.pull-right.direction
        a.btn.btn-block.btn-primary.cursor-pointer(
            ng-if="nextTask"
            ui-sref="index.space.todo({iteration: iterationIdx, todo: nextTask._id })"
            ng-click="$close()"
          )
          span.fa.fa-fw.fa-code
          span Go To Tasks
          span.fa.fa-fw.fa-arrow-right.pull-right.direction
