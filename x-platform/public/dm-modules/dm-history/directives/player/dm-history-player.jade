.full-height(ng-if="historyId")
  dm-recorder-context(recorder='$parent.recorderHistory')
    dm-xplayer-full(
      state='state'
      annotations='annotations'
      recording='recording'
      recorder='recorderHistory'
      audio-url='false'
      on-first-run='autoGoFullScreen()'
      )
