.full-height(ng-if="historyId")
  dm-recorder-context(recorder='$parent.recorder')
    dm-xplayer-full(
      state='state'
      annotations='annotations'
      recording='recording'
      recorder='recorder'
      audio-url='false'
      on-first-run='autoGoFullScreen()'
      )
