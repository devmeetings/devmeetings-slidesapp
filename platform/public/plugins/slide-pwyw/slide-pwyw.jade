div
    h1 Pay What You Want

    div.well.well-lg(ng-hide="fromUrl")
      h2 Your registration ID is unknown please paste it to the field below
      .form-group
        input.form-control(type="text", ng-model="urlEncoded", placeholder="Registration ID from mail")
    iframe(ng-if="url", ng-src="{{ url }}")
