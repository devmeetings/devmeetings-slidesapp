extends ../layouts/main

block content
    .container
        .registration-holder
            form(action="/registration", method="post")
                .col-md-12
                    .registration-header
                        | Rejestrajca
                        span.login-switch.pull-right
                            a(href="/login") 
                                | Logowanie

                    .form-group
                        .alert-error
                            strong #{message}
                    .form-group
                        .ico-holder
                            img.ico-sub.ico-user(src='static/images/login/ico_user.png')
                        input.form-control.form-login(type="text", name="username", placeholder="nazwa użytkownika", required=true)
                    .form-group
                        .ico-holder
                            img.ico-sub.ico-user(src='static/images/login/ico_user.png')
                        input.form-control.form-login(type="email", name="email", placeholder="e-mail", required=true)
                    .form-group
                        .ico-holder
                            img.ico-sub.ico-password(src='static/images/login/ico_password.png')
                        input.form-control.form-login(type="password", name="password", placeholder="hasło", required=true)
                
                .row
                    .col-md-12.text-center.btn-send-holder
                        button.btn.btn-reg(type="submit") Zarejestruj
